import { SelectFields } from 'express-validator/src/select-fields';
import { Request } from 'express-validator/src/base';
import { ContextBuilder } from 'express-validator/src/context-builder';
import { ContextRunner } from 'express-validator/src/chain/context-runner';
export declare class ContextRunnerImpl implements ContextRunner {
    private readonly builder;
    private readonly selectFields;
    constructor(builder: ContextBuilder, selectFields?: SelectFields);
    run(req: Request, options?: {
        saveContext?: boolean;
    }): Promise<import("express-validator/src/context").Context>;
}
